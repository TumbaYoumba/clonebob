.needs_init_and_scanners:
  needs: 
    - job: init
      artifacts: true
    - job: secrets-scan


cleanup:
  extends: 
    - .allow_failure
    - .needs_init
    - .tags
  stage: cleanup
  image: "$CI_REGISTRY/$SEC_PATH_TO_IMAGES/toolset:latest"
  script:
      - python3 /falser/falser.py
